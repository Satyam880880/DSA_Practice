import java.util.*;

public class HashMapCode {
    static class HashMap<k,v>{
        private class Node {
            k key;
            v value;
            Node(k key, v value) {
                this.key = key;
                this.value = value;
            }
        }
         private int size;  // number of elements in the hash map (n)
         public LinkedList<Node> buckets[]; // array of linked lists (buckets) length of bucket

      

      
    }
    public static void main(String[] args) {
        
    }
}
